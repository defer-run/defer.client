// import { useDeferRoute } from "../../src/next/useDeferRoute";
// import helloWorld from "./fixtures/helloWorld";

describe("useDeferRoute()", () => {
  test("1+1", () => {
    expect(1 + 1).toEqual(2);
  });
});

// const [coucou, { loading, result }] = useDeferRoute<typeof helloWorld>('')
